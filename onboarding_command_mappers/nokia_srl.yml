---
sync_devices:
  hostname:
    commands:
      command: "show version | as json"
      parser: "textfsm"
      jpath: "[*].Hostname"
  serial:
    commands:
      command: "show version | as json"
      parser: "textfsm"
      jpath: "[*].Serial Number"
  device_type:
    commands:
      command: "show version | as json"
      parser: "textfsm"
      jpath: "[*].Chassis Type"
